# Retired compatibility pointer.
# The previous file contained JSON despite the .yaml extension.
status: retired
canonical_registry: source_registry.json
reason: extension_mismatch_and_runtime_v2_consolidation
must_not_be_loaded_as_registry: true
