name: mount-check

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mount-check:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: src
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Show runtime versions
        run: |
          python3 --version
          node --version
      - name: Install project and test dependencies
        run: python3 -m pip install --disable-pip-version-check -e '.[dev]'
      - name: Unit tests
        id: unit_tests
        shell: bash
        run: |
          set +e
          make unit > unit-test.log 2>&1
          status=$?
          cat unit-test.log
          exit $status
      - name: Upload unit test log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-log
          path: unit-test.log
      - name: Integration tests
        run: make integration
      - name: Contract tests
        run: make contracts
      - name: Architecture tests
        run: make architecture
      - name: Runtime contract validation
        run: make runtime-contract
      - name: Source registry and OPML validation
        run: make source-opml
      - name: CLI smoke
        run: make cli-smoke
      - name: Run mount integrity check
        run: bash check_mount_integrity.sh