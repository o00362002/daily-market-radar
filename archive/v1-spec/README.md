# Frozen v1 Spec

This directory records that v1 Markdown/YAML quota-driven execution is superseded.
The repo keeps legacy files in place when `brain-core` compatibility or historical
navigation still requires their paths.

Frozen behavior:

- fixed-count completion as evidence of report completeness
- split source identities across media, official/data, and feed files
- manually maintained OPML as canonical source truth
- Markdown tables as canonical potential-signal storage

Active v2 replacements:

- `config/source_registry.yaml`
- `src/radar/`
- `schemas/`
- `migrations/0001_runtime_foundation.sql`
- `make validate`
